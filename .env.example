# Public production origin used for OAuth redirect URLs and share metadata.
SITE_URL=https://your-domain.example

# Configure these as Cloudflare secrets in production; never commit real values.
ENDPOINT_ENCRYPTION_KEY=
SESSION_PEPPER=
ADMIN_TOKEN=
